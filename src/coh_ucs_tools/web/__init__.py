"""CoH UCS Toolkit — web application package.

FastAPI service exposing the existing CLI toolkit (parser, validator,
statistics, merge) as a documented REST API plus a static single-page
frontend. The CLI modules are imported as-is; no logic is reimplemented.
"""
